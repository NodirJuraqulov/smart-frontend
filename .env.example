# Use the origin that served the frontend (recommended for production/reverse proxy).
VITE_API_URL=same-origin

# For development or split-domain installations, use an explicit absolute URL:
# VITE_API_URL=http://localhost:5000
